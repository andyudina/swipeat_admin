{% extends 'editorial/base.html' %}

{% block body %}
<div class="container"><h1>Рестораны</h1></div>
<div id="exTab1" class="container">	
    <ul  class="nav nav-pills">
	   <li class="active">
           <a  href="#new" data-toggle="tab">Новые</a>
	   </li>
	   <li>
	       <a href="#completed" data-toggle="tab">Законченные</a>
	   </li>
	   <li>
	      <a href="#all" data-toggle="tab">Все</a>
	   </li>
    </ul>
    <div class="tab-content clearfix">
	    <div class="tab-pane active" id="new">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>№</th>
                    <th>Название</th>
                </tr>
            </thead>
            <tbody>
            {% for restaurant in new_restaurants %}
                <tr>
                    <td>{{ restaurant.id }}</td>
                    <td><a href="/{{ restaurant.id }}/">{{ restaurant.title }}</a></td>
                </tr> 
            {% endfor %}
            </tbody>
        </table>

	    </div>
		<div class="tab-pane" id="completed">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>№</th>
                    <th>Название</th>
                </tr>
            </thead>
            <tbody>
            {% for restaurant in completed_restaurants %}
                <tr>
                    <td>{{ restaurant.id }}</td>
                    <td><a href="/{{ restaurant.id }}/">{{ restaurant.title }}</a></td>
                </tr> 
            {% endfor %}
            </tbody>
        </table>
		</div>
        <div class="tab-pane" id="all">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>№</th>
                    <th>Название</th>
                    <th>Выполнен</th>
                </tr>
            </thead>
            <tbody>
            {% for restaurant in all_restaurants %}
                <tr>
                    <td>{{ restaurant.id }}</td>
                    <td><a href="/{{ restaurant.id }}/">{{ restaurant.title }}</a></td>
                    <td>
                        <div class="[ form-group ]">
                            <input disabled type="checkbox" name="{{ question.id }}" id="checkbox{{ question.id }}" autocomplete="off"  {% if question.is_true %} checked {% endif %} />
                        <div class="[ btn-group ]">
                           <label for="checkbox{{ question.id }}" class="[ btn btn-primary ]">
                              <span class="[ glyphicon glyphicon-ok ]"></span>
                              <span> </span>
                           </label>
                        </div>
                        </div>
                    </td>
                </tr> 
            {% endfor %}
            </tbody>
        </table>
	    </div>
  </div>



{% endblock body %}	
